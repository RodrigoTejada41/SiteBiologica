---
import { company, whatsappUrl } from '../data/site';

type Props = {
  title?: string;
  text?: string;
};

const {
  title = 'Vamos cuidar do seu ambiente?',
  text = 'Conte o que está acontecendo e receba uma orientação inicial para solicitar seu orçamento.',
} = Astro.props;
---

<section class="section" aria-labelledby="cta-title">
  <div class="container">
    <div class="cta-panel" data-reveal>
      <div>
        <h2 id="cta-title">{title}</h2>
        <p>{text}</p>
      </div>
      <a
        class="button button-light"
        href={whatsappUrl('Olá! Gostaria de solicitar um orçamento para controle de pragas.')}
        target="_blank"
        rel="noopener noreferrer"
      >
        Solicitar orçamento
        <span aria-hidden="true">→</span>
      </a>
    </div>
  </div>
</section>
